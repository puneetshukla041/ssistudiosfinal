"use client";

import LoginLayout from "@/components/login/LoginLayout";

/**
 * Main entry component for the Login Page.
 * Renders the LoginLayout which contains all the logic and UI components.
 */
export default function LoginPage() {
  return <LoginLayout />;
}